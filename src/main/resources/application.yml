
server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
   driver-class-name: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql:///spring-test?serverTimezone=UTC
   username: root
   password: 7718
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: sspu.edu.eduplatform.entity
  configuration:
    map-underscore-to-camel-case: true
